#include <iostream>
#include "Graph.h"
#include "InputMode.h"

int main(int argc, char ** argv) {
    InputMode i(argc, argv);
    /*std::cout << "add graph" << std::endl;
    int n;
    std::cin >> n;
    Graph g(n);
    int a ,b;
    while(n > 0){
        std::cin>>a>>b;
        g.addEdge(a,b);
        --n;
    }
    g.writeEdges();*/
    return 0;
}